```
# apt-upgrader
a simple gui for upgrading packages for apt-get using apt-get dist-upgrade

# Depends
fltk
apt-get

# Building from source
git clone https://github.com/Git-Fal7/apt-upgrader/
cd apt-upgrader/
fltk-config --compile apt-upgrader.cxx
```
